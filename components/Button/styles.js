import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    flex: 1,
    margin: 5,
    backgroundColor: '#a93',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 5,
  },
  label: {
    fontSize: 20,
  },
});
