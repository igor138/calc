import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  digit: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
  },
  text: {
    fontSize: 50,
  },
});
